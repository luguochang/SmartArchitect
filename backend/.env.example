# Backend Environment Variables Configuration
#
# 本地开发使用说明：
# 1. 复制本文件为 .env: cp .env.example .env
# 2. 根据需要修改配置项
# 3. AI API Keys 可选，可通过 UI 配置
#
# 生产环境部署说明（Render）：
# - 不要使用 .env 文件，请在 Render Dashboard 中配置环境变量
# - 详见 render.yaml 和 docs/DEPLOYMENT_GUIDE.md

# ==================== API Configuration ====================
# API 服务器配置
API_HOST=0.0.0.0
API_PORT=8003
API_RELOAD=True

# ==================== CORS Configuration ====================
# CORS 允许的前端域名（逗号分隔，不要有空格）
# 生产环境示例: https://your-app.vercel.app,https://your-domain.com
CORS_ORIGINS_STR=http://localhost:3000,http://127.0.0.1:3000

# ==================== Environment & Logging ====================
# 环境模式: development | production
ENVIRONMENT=development

# 日志级别: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# ==================== AI Model Configuration ====================
# AI API Keys (可选 - 可通过 UI 配置)
#
# Google Gemini (用于 Vision 分析、Chat Generator)
# GEMINI_API_KEY=your-gemini-api-key
#
# OpenAI GPT (用于 Vision 分析)
# OPENAI_API_KEY=your-openai-api-key
#
# Anthropic Claude (用于 Vision 分析)
# ANTHROPIC_API_KEY=your-anthropic-api-key
#
# SiliconFlow (用于 Excalidraw 生成)
# SILICONFLOW_API_KEY=your-siliconflow-api-key
# SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
